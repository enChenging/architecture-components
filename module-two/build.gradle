apply from: '../gradle-script/base-module.gradle'

android {
    defaultConfig {
        manifestPlaceholders = [
                JPUSH_PKGNAME: "${AndroidConfig.PACKAGE_NAME}.two",
                JPUSH_APPKEY : "",
                JPUSH_CHANNEL: "",
        ]
    }
    resourcePrefix "two_"
}